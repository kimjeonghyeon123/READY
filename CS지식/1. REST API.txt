1. REST API
  1) API에서 자원들은 각각의 독립적인 인터페이스를 가짐
    - html버전이 바뀐다고 http 버전을 바꿔야될 필요는 없다.
  
  2) 자원은 url로 식별되어야 함
    - /product/{id}

  3) url과 GET, DELETE 등 HTTP 메서드를 통해 자원에 대한 작업을 설명할 수 있는
     정보가 담겨야 한다.

  4) 