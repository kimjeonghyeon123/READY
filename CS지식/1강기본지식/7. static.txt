1. static 사용 이유
  1) 클래스의 인스턴스가 아닌 클래스에 속하며 클래스의 변수, 메서드 등을
     공유하는데 사용됩니다.
  2) 이를 통해 해당 클래스로 만들어진 객체사이에서 중복되는 메서드, 속성을
     효율적으로 정의할 때 쓰입니다.
  3) 단점
    - 선언하자마자 메모리에 올라가게 되서 사용하지 않을 경우 메모리 낭비
    - 힙이 아니라 method 영역에 할당이 되서 종료될 때까지 해제가 안됨
